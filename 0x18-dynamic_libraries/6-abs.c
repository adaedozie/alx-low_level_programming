#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @r: integer to be computed
 * Return: absolut value of the integer
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (-r);
}

